spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://localhost:3306/user_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

